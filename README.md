# DiseaseRiskPredictor
The app takes patient vitals as input (age, BMI, glucose, blood pressure, insulin), runs them through an ML.NET FastTree binary classifier trained on the Pima Indians Diabetes dataset, and returns a risk probability + category (Low / Medium / High) via an ASP.NET Razor Pages UI.
